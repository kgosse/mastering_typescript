import {ConfigStructure} from "./structure/config-structure";

const CONFIG: ConfigStructure = {
    SERVER_URL: "http://localhost:8082",
    DATE: {
        LONG_FORMAT: "Do MMM YYYY",
        SHORT_FORMAT: "MM/DD/YYYY"
    }
};

export default CONFIG;
